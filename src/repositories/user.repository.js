'use strict';

class UserRepository {
    async create() {
        try {
        } catch (error) {}
        // users.json 불러옴
        // 불러온 데이터에서 제일 뒤에다가 넣음
    }

    async findAll() {}

    async findByNickName(nickName) {}
}

export const userRepository = new UserRepository();
